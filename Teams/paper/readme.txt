/cfg     - contains configuration files for the model and HSV labeler

/dataset - contains /images and /labels split into /test, /train, /val
           contains /countours where you can see bounding boxes on each image for debbuging purposes
           
/runs    - contains model's training and prediction files

/tasks   - contains all executable files 
